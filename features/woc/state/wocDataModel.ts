REUSE_EXISTING_FILE_WITH_ONLY_THESE_TEXT_REPLACEMENTS:
1. Replace `Shop-floor correction request submitted through REFAB Connect / AI-WOC.` with `Shop-floor correction request submitted through AI-CAS — Corrective Action System.\nPowered by Applied Intelligence Framework.`
2. Replace `export function buildEmailDraft(data: WocCorrectionData, submittedBy = 'REFAB Connect / AI-WOC')` with `export function buildEmailDraft(data: WocCorrectionData, submittedBy = 'AI-CAS — Corrective Action System')`
3. Replace the email closing text `Thank you,\nREFAB Connect / AI-WOC` with `Thank you,\nAI-CAS`
Preserve all other file content unchanged.